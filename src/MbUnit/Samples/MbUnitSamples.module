<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003" ToolsVersion="3.5">
  <Import Project="$(MetaBuildBinDir)\MetaBuild.Module.targets" />

  <ItemGroup>
    <CSharpProject Include="MbUnit.Samples\MbUnit.Samples.csproj" />
    <CSharpProject Include="MbUnit.Samples.VSTSIntegration\MbUnit.Samples.VSTSIntegration.csproj" />

    <ExcludedFile Include="*.module" />
    <ExcludedFile Include="*.wxs" />
  </ItemGroup>

  <Target Name="AfterImage">
    <RemoveDir Directories="$(ImageDir)\docs\Samples" />
    <MakeDir Directories="$(ImageDir)\docs" />
    
    <SvnExport RepositoryPath="$(ModuleDir)"
               LocalPath="$(ImageDir)\docs\Samples" />

    <Delete Files="@(ExcludedFile->'$(ImageDir)\docs\Samples\%(Identity)')"
            Condition="'@(ExcludedFile)'!=''" />
  </Target>
</Project>
