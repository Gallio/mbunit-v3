using System;
using System.Collections.Generic;
using System.Runtime.CompilerServices;
using System.Text;

namespace MbUnit.Framework.ContractVerifiers
{
    /// <summary>
    /// The MbUnit.Framework.ContractVerifiers namespace contains contracts (<see cref="IContract" />)
    /// that are used together with the <see cref="VerifyContractAttribute" /> to verify
    /// common invariants that appear in contracts.
    /// </summary>
    /// <remarks>
    /// <para>
    /// For example the <see cref="EqualityContract{T}" /> ensures that the <see cref="Object.Equals(object)"/>
    /// and <see cref="Object.GetHashCode" /> methods and the equality operators produce consistent and
    /// correct results for the provided values.  Likewise the <see cref="ExceptionContract{T}" /> ensures
    /// that custom exception types are correctly implemented.
    /// </para>
    /// </remarks>
    [CompilerGenerated]
    class NamespaceDoc
    {
    }
}
