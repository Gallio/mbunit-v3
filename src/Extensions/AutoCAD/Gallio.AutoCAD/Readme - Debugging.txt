How to Debug
============

The AutoCAD plugin assumes that Gallio.Loader has been registered in the GAC.
Be sure to have AutoCAD installed and registered before proceeding.

1. Run the "Install.bat" batch file from the command-line to
   set registry keys and install assemblies in the GAC.

2. Run the tests.
