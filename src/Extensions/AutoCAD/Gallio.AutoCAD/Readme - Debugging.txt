How to Debug
============

The AutoCAD plugin assumes that Gallio.Loader has been registered in the
GAC and that the plugin components have been installed in AutoCAD.

1. Run the "Install.bat" batch file from the command-line to
   set registry keys and install assemblies in the GAC.

2. Copy the plugin into the AutoCAD plugins folder.