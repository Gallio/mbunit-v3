How to Debug
============

The AutoCAD plugin assumes that it can find the Gallio runtime path
via conventional methods (see GallioLoader). Be sure to have AutoCAD
installed and registered before proceeding.

1. Run the "Install.bat" batch file from the command-line to
   set registry keys.

2. Run the tests.
